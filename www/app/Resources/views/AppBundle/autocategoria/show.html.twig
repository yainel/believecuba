{% extends 'base.html.twig' %}

{% block body %}
    <h1>Autocategorium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ autoCategorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ autoCategorium.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ autoCategorium.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('autocategoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('autocategoria_edit', { 'id': autoCategorium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
